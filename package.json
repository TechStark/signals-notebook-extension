{
  "name": "signals-notebook-extension",
  "version": "0.1.0",
  "description": "Chrome extension providing enhanced features for Signals Notebook",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.7.1",
    "@types/chrome": "^0.0.280",
    "@types/node": "^20.0.0",
    "esbuild": "^0.28.1",
    "typescript": "^5.6.0",
    "vite": "^7.0.0"
  }
}
